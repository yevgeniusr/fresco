import Link from "next/link";
import { ArrowUpRight, Clock3, Users } from "lucide-react";
type ExperimentCardProps = { experiment: { id: string; slug: string; title: string; question: string; status: string; domain: string; durationDays: number; participantTarget: number; participantCount: number; isDemo: boolean; }; hrefPrefix?: "/experiments" | "/app/experiments"; };
export function ExperimentCard({ experiment, hrefPrefix = "/experiments" }: ExperimentCardProps) {
  return <article className="paper group grid min-h-72 content-between p-5 transition-transform hover:-translate-y-1 sm:p-6"><div><div className="flex flex-wrap items-center justify-between gap-2"><span className="eyebrow">{experiment.domain}</span><span className="tag gap-2"><span className="status-dot" />{experiment.status}</span></div><h3 className="serif mt-7 text-2xl font-bold leading-tight tracking-[-.025em]">{experiment.title}</h3><p className="mt-3 text-sm leading-6 text-[var(--muted)]">{experiment.question}</p></div><div>{experiment.isDemo ? <p className="mb-4 text-[.67rem] font-bold uppercase tracking-[.1em] text-[var(--oxide-dark)]">Illustrative demo data</p> : null}<div className="flex items-center justify-between border-t hairline pt-4 text-xs font-bold"><span className="flex gap-4 text-[var(--muted)]"><span className="flex items-center gap-1"><Clock3 size={13} />{experiment.durationDays} days</span><span className="flex items-center gap-1"><Users size={13} />{experiment.participantCount}/{experiment.participantTarget}</span></span><Link href={`${hrefPrefix}/${experiment.slug}`} className="flex items-center gap-1 no-underline">Inspect <ArrowUpRight size={14} /></Link></div></div></article>;
}
