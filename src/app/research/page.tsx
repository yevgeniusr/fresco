import type { Metadata } from "next";
export const metadata: Metadata = { title: "Research" };
export default function ResearchPage() {
  return <div className="shell py-20"><p className="eyebrow">Research desk · early</p><h1 className="serif mt-6 max-w-4xl text-6xl font-bold tracking-[-.055em]">Evidence before certainty.</h1><p className="mt-7 max-w-3xl text-xl leading-9 text-[var(--muted)]">Fresco’s research layer will connect external evidence, datasets, prior interventions, contradictions, and open questions to each hypothesis.</p><div className="mt-16 grid gap-5 md:grid-cols-3">{[["Health","How do social context, access, enjoyment, and routine design affect sustained movement?"],["Learning","When do small quests support intrinsic motivation—and when do they crowd it out?"],["Social","Which designed activities create mutual attention instead of shallow contact?"]].map(([title,question]) => <article key={title} className="paper p-7"><span className="eyebrow">Open question</span><h2 className="serif mt-8 text-3xl font-bold">{title}</h2><p className="mt-4 leading-7 text-[var(--muted)]">{question}</p></article>)}</div><div className="alert mt-12"><strong>Evidence boundary:</strong> Fresco’s seeded experiment measurements are explicitly synthetic demo data. They are not research findings and must not be cited as such.</div></div>;
}
