import type { Metadata } from "next";
import { MethodLoop } from "@/components/method-loop";

export const metadata: Metadata = { title: "The Method" };
const spec = [["Observation","What is happening now?"],["Goal","Which human outcome should improve?"],["Hypothesis","Which environmental change might cause improvement?"],["Intervention","What changes, exactly?"],["Participants","Who knowingly opts in, and who is excluded?"],["Baseline","What happens before the change?"],["Primary metric","Which single measure most directly tests the hypothesis?"],["Secondary metrics","What adds context without moving the goalposts?"],["Guardrails","What must not get worse?"],["Duration","How long is enough to learn without overexposure?"],["Data collection","What is collected, and why is each field necessary?"],["Privacy","What becomes public, and what stays private?"],["Result","What happened—including negative effects?"],["Limitations","Why might this result mislead us?"],["Decision","Scale, Modify, Repeat, Stop, or Inconclusive?"]] as const;
export default function MethodPage() {
  return <div className="shell py-20"><p className="eyebrow">The Fresco Method</p><h1 className="serif mt-6 max-w-5xl text-6xl font-bold leading-[.95] tracking-[-.055em]">From a problem people should fix to an environment we can test.</h1><p className="mt-7 max-w-3xl text-xl leading-9 text-[var(--muted)]">The method is more important than any Fresco feature. Anyone should be able to use it without the software.</p><div className="mt-14"><MethodLoop /></div><section className="mt-20 grid gap-10 lg:grid-cols-[.7fr_1.3fr]"><div><p className="eyebrow">Experiment specification</p><h2 className="serif mt-5 text-4xl font-bold tracking-[-.04em]">A shared grammar for learning.</h2><p className="mt-5 leading-7 text-[var(--muted)]">Structure does not make a weak experiment rigorous, but it makes gaps, tradeoffs, and uncertainty visible enough to challenge.</p></div><dl className="border-t border-[var(--line)]">{spec.map(([name, question]) => <div key={name} className="grid gap-2 border-b hairline py-5 sm:grid-cols-[10rem_1fr]"><dt className="font-bold">{name}</dt><dd className="m-0 text-[var(--muted)]">{question}</dd></div>)}</dl></section><blockquote className="serif mx-auto mt-20 max-w-4xl border-y border-[var(--line)] py-12 text-center text-4xl font-bold leading-tight tracking-[-.035em]">“A precise failure teaches more than a vague success.”</blockquote></div>;
}
